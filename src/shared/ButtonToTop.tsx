import { memo } from "react";

const ButtonToTop = () => {
    return (
        <a href="#" className="btn btn-primary back-to-top"><i className="fa fa-angle-up"></i></a>
    )
}
export default memo(ButtonToTop);
